{"date":"2026-09-20","from":{"k":"unit","s":"shell","n":"XSchedulerGraph > static members > #truncateLabel() > given a schemed entity segment > cuts the assembled label to a given `maxLen`"},"to":{"k":"unit","s":"shell","n":"XSchedulerGraph > static members > #truncateLabel() > given a label whose assembled form is longer than `maxLen` > returns the prefix and the entity tail alone when the two are exactly `maxLen` long"}}
