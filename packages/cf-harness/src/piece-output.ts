/** Completion contract for a parent task that must leave an openable piece. */
export const PIECE_OUTPUT_GUIDANCE =
  "Host completion contract: a completed task must leave a user-facing piece. If the answer is text, make the smallest pattern that renders that answer. Use the ordinary authoring and run_pattern path, then assign_slug on its resultRef; a data-only probe is not the answer. For an existing piece, confirm its existing slug with assign_slug after the revision, or give an unnamed piece a slug. A successful assign_slug in this run is required before a final answer, and its pending/UI checks still apply. Respect result inspection limits: never claim unseen rows, counts, or matches. If a required input or choice is missing, call finish_task with question; if the task cannot proceed, call it with gave-up and the concrete reason. Those are the piece-less endings; do not replace them with a plain-text completed answer.";
